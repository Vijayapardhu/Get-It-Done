name: getitdone_customer
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.8.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The design system and the platform plumbing, extracted when the worker app
  # arrived so there is exactly one token scale and one auth refresh queue for
  # both apps. See packages/gid_ui and packages/gid_core.
  gid_ui:
    path: ../packages/gid_ui
  gid_core:
    path: ../packages/gid_core
  # Locale-aware Material/Cupertino strings (date pickers, dialogs) and the
  # delegates AppLocalizations is generated against.
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  dio: ^5.11.0
  flutter_riverpod: ^3.3.2
  go_router: ^17.0.0
  flutter_secure_storage: ^10.3.1
  intl: ^0.20.2
  # Opening the phone dialler for the assigned worker, and the invoice PDF
  # in the system browser.
  url_launcher: ^6.3.1
  # Saving the invoice PDF out of the app — a receipt you cannot send to
  # anyone is not a receipt.
  path_provider: ^2.1.5
  share_plus: ^10.1.4
  geolocator: ^14.0.2
  socket_io_client: ^3.1.6
  google_sign_in: ^7.2.0
  # Razorpay checkout. Native SDK — there is no Flutter-side fallback, so the
  # payment screen degrades to a simulated flow when the backend reports the
  # gateway is not configured.
  razorpay_flutter: ^1.4.5
  clock: ^1.1.2
  lottie: ^3.3.1
  cached_network_image: ^3.4.1
  phosphor_flutter: ^2.1.0
  flutter_local_notifications: ^20.1.0
  google_maps_flutter: ^2.14.2
  # Remote push. flutter_local_notifications above only draws a notification
  # this app asked for while it is running; these two are what let the server
  # reach a device whose app is closed.
  firebase_core: ^3.8.0
  firebase_messaging: ^15.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # Generates lib/l10n/app_localizations.dart from the .arb files. Not
  # committed -- `flutter gen-l10n` runs in CI and on every `flutter pub get`.
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Bundled rather than fetched at runtime. google_fonts downloads on first
  # launch, which means a font flash on a cold start and a hard network
  # dependency for something as fundamental as the app's typeface.
  fonts:
    - family: PlusJakartaSans
      fonts:
        - asset: assets/fonts/PlusJakartaSans-400.ttf
          weight: 400
        - asset: assets/fonts/PlusJakartaSans-500.ttf
          weight: 500
        - asset: assets/fonts/PlusJakartaSans-600.ttf
          weight: 600
        - asset: assets/fonts/PlusJakartaSans-700.ttf
          weight: 700
    - family: NotoSansTelugu
      fonts:
        - asset: assets/fonts/NotoSansTelugu-400.ttf
          weight: 400
        - asset: assets/fonts/NotoSansTelugu-500.ttf
          weight: 500
        - asset: assets/fonts/NotoSansTelugu-600.ttf
          weight: 600
        - asset: assets/fonts/NotoSansTelugu-700.ttf
          weight: 700
    - family: NotoSansDevanagari
      fonts:
        - asset: assets/fonts/NotoSansDevanagari-400.ttf
          weight: 400
        - asset: assets/fonts/NotoSansDevanagari-500.ttf
          weight: 500
        - asset: assets/fonts/NotoSansDevanagari-600.ttf
          weight: 600
        - asset: assets/fonts/NotoSansDevanagari-700.ttf
          weight: 700

  assets:
    # The brand mark and wordmark, generated by tool/generate_brand_assets.py.
    # Regenerate rather than editing the PNGs: the colours come from
    # lib/design/tokens/colors.dart and must not drift from them.
    - assets/brand/
    # Loop animations, generated by tool/generate_lottie.py. Same rule:
    # regenerate rather than editing the JSON by hand.
    - assets/lottie/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
