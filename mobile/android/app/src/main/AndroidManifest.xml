<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Every screen in this app talks to the API. Until now INTERNET was
         declared only in the debug and profile manifests, which Flutter ships
         for the tooling's benefit - so debug builds worked and a release APK
         had no network at all. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Android 13 (API 33) put notifications behind a runtime grant. Without
         this declared, requestNotificationsPermission() returns false without
         ever showing a dialog, and every booking update is silently dropped. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Finding workers near you, and filling an address from where you are
         standing. COARSE is declared alongside FINE because Android 12+ lets
         the user grant only approximate location, and an app that asks for
         FINE alone gets nothing when they choose "Approximate". Street-level
         accuracy is not needed to pick a service area, so approximate is a
         perfectly good answer here.

         Foreground only. This app never tracks a customer in the background —
         it is the WORKER's app that shares position during a job. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:label="GET IT DONE"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/${NETWORK_SECURITY_CONFIG}">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Google Maps SDK for Android. The value comes from MAPS_API_KEY in
             local.properties via a manifest placeholder, so the key is not in
             the repository. Restrict it to this package name and signing
             certificate in the Cloud console: a key inside an APK is readable
             by anyone who has the APK. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Android 11+ package visibility. Without these, url_launcher's
             canLaunchUrl returns false and calling the assigned worker or
             sharing a receipt silently does nothing. -->
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND"/>
            <data android:mimeType="*/*"/>
        </intent>
    </queries>
</manifest>
